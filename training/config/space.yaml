title: WarGames Demo
emoji: "🛡️"
sdk: docker
app_port: 8000
space_repo_id: your-org/wargames-demo-space
artifact_mode: adapter
# Model identifiers used by the Space at runtime. Both also accepted via
# Space secrets (Space Settings -> Repository Secrets) which override these
# values; surface them here so the deployment config is single-sourced.
model_repo_id: your-org/wargames-red-lora
base_model_name: Qwen/Qwen3.5-9B
